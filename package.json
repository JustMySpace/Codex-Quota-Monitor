{
  "name": "codex-quota-monitor",
  "private": true,
  "version": "0.1.0",
  "license": "WTFPL",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:win": "scripts\\with-msvc.cmd tauri dev",
    "dev:mac": "tauri dev",
    "dev:frontend": "vite --host 127.0.0.1",
    "build": "tauri build",
    "build:win": "scripts\\with-msvc.cmd tauri build",
    "build:mac": "tauri build",
    "build:frontend": "tsc && vite build",
    "tauri": "tauri",
    "tauri:win": "scripts\\with-msvc.cmd tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-autostart": "^2.5.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
