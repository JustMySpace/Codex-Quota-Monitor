# 更新日志

[English](CHANGELOG.md)

本文件记录项目的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- 新增登录时自动启动 checkbox，通过 Windows、macOS 和 Linux 的系统自启机制启用或关闭。
- 每 15 秒通过本机实验性 Codex app-server 读取当前主额度，失败时回退到 session 日志快照。
- 记忆浮窗位置，为主面板新增显式置顶开关，并支持通过托盘菜单或双击托盘图标打开主面板。

### 修复

- 修复额度燃尽图 X 轴标签重叠，为 Start/Reset 增加独立边界线、单独的 Now 标记，并显示每天的日期与时间刻度。
- 使用本地 SQLite 账本持久保存 Token 元数据，支持增量去重和主动清除历史，源对话删除后已记录用量不再回退。
- 同时扫描活动与已归档的 Codex 会话日志，避免归档对话后今日 Token 总量回退。
- 普通模型按 7 天周期选择主周额度，无论额度位于 `primary` 还是 `secondary`。
- 根据 Session 模型和上下文信息忽略 Spark 独立额度快照。
- 明确选择 app-server 返回的 `codex` 主额度桶，避免独立 Spark 额度覆盖主额度。
- 当最新 Session 事件来自 Spark 时，保留最近一次有效的主周额度。
- 没有有效主周额度时显示未知状态，不再显示虚假的 `100%`。
- 当程序在 reset 之后才启动时，燃尽图仍从 reset 时刻的 `100%` 开始绘制。
- 普通模型的 7 天额度无论位于哪个 rate-limit 字段都可以识别，同时按模型/上下文信息排除 Spark 会话。
- 忽略已经过期的 reset 快照，避免燃尽图绘制超过 7 天的旧周期。

## [0.1.0] - 2026-07-16

### 新增

- 本地读取 Codex 会话日志，不向服务器上传用量数据。
- 按分钟统计新输入、缓存输入、输出和推理输出 Token。
- 今日 Token 累计曲线。
- 最近一小时实时 Token 消耗曲线。
- 五分钟分段堆叠 Token 图。
- 支持紧凑和展开两种状态的浮窗监控。
- 双击浮窗打开完整监控面板。
- 点击浮窗右侧小组件，在五分钟消耗和今日累计曲线之间切换。
- 浮窗和主面板拖拽移动。
- 右键调整浮窗透明度，且不会影响主面板。
- 系统托盘菜单，支持显示/隐藏、透明度和退出。
- 显示剩余额度、重置周期用量、最近会话和上下文限制。
- 从上次重置到下次重置的额度燃尽图。
- 可配置燃尽图的使用日期，默认启用每天。
- 支持跟随系统以及手动切换暗色和亮色主题。
- 支持系统语言检测，并可手动切换十种常见语言。
- 提供英文和简体中文文档及截图。
- 支持 Windows NSIS 安装包和可执行文件构建。
- 支持 macOS 源码构建和打包。
- 添加 WTFPL 开源许可证。

### 变更

- 浮窗始终显示今日 Token 总消耗和剩余百分比。
- 浮窗透明度不再影响完整监控面板。
- 紧凑浮窗切换显示模式时，时间和状态行位置保持稳定。
- 移除紧凑浮窗中多余的“double click panel”提示。
- 扩大浮窗可拖拽区域，同时保留按钮点击和双击行为。
- 浮窗正常悬停时不再显示移动/调整大小光标。
- 主面板采用无边框布局，移除之前的透明外框。
- 修正堆叠图图例的颜色标识和文字显示。

### 修复

- 修复额度百分比可能因按文件顺序选择限流事件而显示旧值的问题，现按事件时间戳选择最新数据。
- 修复浮窗切换显示模式时文字和图表内容可能互相遮挡的问题。
- 修复拖拽操作可能与浮窗双击操作冲突的问题。
- 修复托盘菜单中透明度选项层级不合理的问题。

### 构建

- 已验证 Windows release 构建和 NSIS 安装包构建。
- 生成的产物：
  - `src-tauri/target/release/codex-quota-monitor.exe`
  - `src-tauri/target/release/bundle/nsis/Codex Quota Monitor_0.1.0_x64-setup.exe`

[0.1.0]: https://github.com/JustMySpace/Codex-Quota-Monitor/releases/tag/v0.1.0
